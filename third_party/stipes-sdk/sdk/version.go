package sdk

// Version is the SDK release version. Follows semver.
const Version = "0.1.0"

// ProtocolVersion identifies the plugin handshake protocol.
// A core and plugin must share the same protocol version to be compatible.
const ProtocolVersion = "fungobox_plugin_v2"
